SELECT 	ENAME, JOB, HIREDATE
FROM 	EMP


SUBSTR(CAMPO, POS_INIC, QTD_POS)

SUBSTR(CAMPO, POS_INIC)

SELECT 	ENAME, JOB, HIREDATE, SUBSTR(HIREDATE,1, 5) AS DIA_MES,
SUBSTR(HIREDATE, 4, 2) MES, SUBSTR(HIREDATE, 4, 5) MES_ANO, 
SUBSTR(HIREDATE, 4) MES_ANO
FROM 	EMP

SELECT ENAME, JOB, SUBSTR(JOB, 2, 4), SUBSTR(JOB, 2)
FROM EMP

SELECT EMPNO, ENAME, JOB, HIREDATE
FROM EMP
WHERE HIREDATE LIKE '__/02/__'
--OU
SELECT EMPNO, ENAME, JOB, HIREDATE
FROM EMP
WHERE HIREDATE SUBSTR(HIREDATE, 4, 2) = '02'

SELECT ENAME, JOB, HIREDATE, HIREDATE + 10
FROM EMP

SELECT ENAME, (SYSDATE - HIREDATE) / 7, 
(TO_DATE('25/12/22') - SYSDATE) / 7 AS SEMANAS
FROM EMP

SELECT ENAME, HIREDATE, ADD_MONTHS(HIREDATE, 8)
FROM EMP

SELECT ENAME, HIREDATE, ADD_MONTHS(HIREDATE, -8)
FROM EMP

SELECT ENAME, JOB, MONTHS_BETWEEN(TO_DATE('25/12/22'), SYSDATE)
FROM EMP

SELECT ENAME, JOB, HIREDATE, TO_CHAR(HIREDATE, 'DD/MON/YYYY')
FROM EMP

SELECT ENAME, JOB, TO_CHAR(HIREDATE, 'DAY - DD/MONTH/YYYY')
FROM EMP

SELECT ENAME, JOB, TO_CHAR(HIREDATE, 'DAY - DD de MONTH de YYYY')
FROM EMP

SELECT INITCAP(ENAME), JOB, INITCAP(TO_CHAR(HIREDATE, 'DAY - DD "de" MONTH "de" YYYY'))
FROM EMP

SELECT INITCAP(ENAME), JOB, INITCAP(TO_CHAR(HIREDATE, 'DAY - DD/MONTH/YYYY'))
FROM EMP

SELECT ENAME, JOB, INITCAP(TO_CHAR(HIREDATE, 'DAY - DD')) || ' de ' ||
INITCAP(TO_CHAR(HIREDATE, 'MONTH')) || ' de ' || TO_CHAR(HIREDATE, 'YYYY')
FROM EMP

SELECT ENAME,INITCAP(TO_CHAR(HIREDATE, 'DAY - DD')) || ' de ' ||
INITCAP(TO_CHAR(HIREDATE, 'MONTH')) || ' de ' || TO_CHAR(HIREDATE, 'YYYY') AS "DATA ADMISSÃO"
FROM EMP

INSERT INTO EMP
(EMPNO, ENAME, SAL, HIREDATE)
VALUES
(1, 'LAURA', 3900, '03/07/22')

INSERT INTO EMP
(EMPNO, ENAME, SAL, HIREDATE)
VALUES
(1, 'LAURA', 3900, '07/03/22')

INSERT INTO EMP
(EMPNO, ENAME, SAL, HIREDATE)
VALUES
(2, 'LAURA', 3900, TO_DATE('07/03/22', 'MM/DD/YY'))
